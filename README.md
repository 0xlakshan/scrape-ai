# Agent Scrape

A simple CLI tool that uses **Puppeteer** and **Google Gemini** to summarize web pages.  
It loads a page, extracts the main content, cleans out noise, and generates a concise AI summary.

You can summarize a single URL, process multiple URLs, follow internal links, or compare summaries across pages.
